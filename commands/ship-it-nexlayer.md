---
description: Ship this project to Nexlayer — build, push, validate, deploy, return the live URL.
argument-hint: "[path or app description]"
---

Invoke the `ship-it-nexlayer` skill for $ARGUMENTS.

If no argument was given, use the current working directory. Follow the skill's steps 0-10 in order; `nexlayer_validate_yaml` must pass before `nexlayer_deploy`. Finish with the live URL.
