---
description: Ship this project to Nexlayer — build, push, validate, deploy, and return the live URL.
argument-hint: "[path or app description]"
---

Use the `nexlayer-deploy` skill to ship $ARGUMENTS to Nexlayer.

If no argument was given, deploy the current working directory. Confirm the app name before deploying, then report the live URL, the services that came up, and anything the user still needs to configure.
