---
description: Launch a pre-built Nexlayer AI app — chatbot, code reviewer, copilot, multi-agent workspace, translation hub, or content moderation.
argument-hint: "[sandbox name or use case]"
---

Use the `nexlayer-ai-sandbox` skill for $ARGUMENTS.

List the catalog first if the request is vague. Confirm which sandbox and what it costs in credits before deploying, then return the live URL.
